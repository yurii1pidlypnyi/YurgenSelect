const select = new YurgenSelect(".select", {});

const select1 = new YurgenSelect(".select-1", {
  search: true,
  allowClear: true,
  change: (item) => {
  },
  ready: (item) => {
  }
});

const select2 = new YurgenSelect(".select-3", {
  multiply: true,
  search: true,
  change: (item) => {
  },
  ready: (item) => {
  }
});